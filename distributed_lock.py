"""\nModule: distributed_lock\nDescription: DynamoDB-backed distributed lock for ensuring mutual exclusion\n    in concurrent bet settlement operations. Implements conditional writes\n    with TTL expiry and exponential backoff retry.\nAuthor: AI Virtual Engineer\nDate: 2026-08-26\nJira: SCRUM-2\n"""\n\nimport time\nimport uuid\nfrom typing import Optional\n\nimport boto3\nimport structlog\nfrom botocore.exceptions import ClientError\n\nfrom config import SettlementConfig\nfrom exceptions import DistributedLockError\n\nlogger = structlog.get_logger(__name__)\n\n# Constants\nMAX_RETRY_ATTEMPTS: int = 3\nBASE_BACKOFF_SECONDS: float = 0.1\nLOCK_TTL_SECONDS: int = 30\n\n\nclass DistributedLock:\n    """DynamoDB-backed distributed lock with TTL expiry.\n\n    Uses conditional writes to ensure only one process can hold the lock\n    for a given resource at any time. Implements exponential backoff retry\n    to handle transient contention.\n\n    Attributes:\n        table_name: DynamoDB table used for lock storage.\n        dynamodb: Boto3 DynamoDB resource client.\n        table: DynamoDB Table resource.\n    """\n\n    def __init__(\n        self,\n        table_name: Optional[str] = None,\n        dynamodb_resource: Optional[object] = None,\n    ) -> None:\n        """Initialize the distributed lock.\n\n        Args:\n            table_name: Name of the DynamoDB lock table.\n                Defaults to SettlementConfig.LOCK_TABLE_NAME.\n            dynamodb_resource: Optional pre-configured DynamoDB resource\n                for testing/injection.\n        """\n        self.table_name = table_name or SettlementConfig.LOCK_TABLE_NAME\n        self.dynamodb = dynamodb_resource or boto3.resource(\"dynamodb\")\n        self.table = self.dynamodb.Table(self.table_name)\n\n    def acquire(\n        self,\n        resource_id: str,\n        owner_id: Optional[str] = None,\n        ttl_seconds: int = LOCK_TTL_SECONDS,\n        max_attempts: int = MAX_RETRY_ATTEMPTS,\n    ) -> str:\n        """Acquire a distributed lock for the given resource.\n\n        Uses DynamoDB conditional put to atomically claim the lock.\n        Retries with exponential backoff on contention.\n\n        Args:\n            resource_id: Unique identifier for the resource to lock\n                (e.g., bet_id).\n            owner_id: Unique identifier for the lock owner. Auto-generated\n                if not provided.\n            ttl_seconds: Time-to-live for the lock in seconds.\n            max_attempts: Maximum number of acquisition attempts.\n\n        Returns:\n            The owner_id that holds the lock.\n\n        Raises:\n            DistributedLockError: If the lock cannot be acquired after\n                max_attempts.\n        """\n        owner_id = owner_id or str(uuid.uuid4())\n        expiry_time = int(time.time()) + ttl_seconds\n\n        for attempt in range(1, max_attempts + 1):\n            try:\n                self.table.put_item(\n                    Item={\n                        \"resource_id\": resource_id,\n                        \"owner_id\": owner_id,\n                        \"expiry_time\": expiry_time,\n                        \"acquired_at\": int(time.time()),\n                    },\n                    ConditionExpression=(\n                        \"attribute_not_exists(resource_id) OR \"\n                        \"expiry_time < :now\"\n                    ),\n                    ExpressionAttributeValues={\n                        \":now\": int(time.time()),\n                    },\n                )\n                logger.info(\n                    \"lock_acquired\",\n                    resource_id=resource_id,\n                    owner_id=owner_id,\n                    attempt=attempt,\n                )\n                return owner_id\n\n            except ClientError as exc:\n                if (\n                    exc.response[\"Error\"][\"Code\"]\n                    == \"ConditionalCheckFailedException\"\n                ):\n                    if attempt == max_attempts:\n                        logger.warning(\n                            \"lock_acquisition_failed\",\n                            resource_id=resource_id,\n                            attempts=max_attempts,\n                        )\n                        raise DistributedLockError(\n                            resource_id=resource_id,\n                            message=(\n                                f\"Failed to acquire lock for \"\n                                f\"'{resource_id}' after \"\n                                f\"{max_attempts} attempts\"\n                            ),\n                        ) from exc\n\n                    backoff = BASE_BACKOFF_SECONDS * (2 ** (attempt - 1))\n                    logger.debug(\n                        \"lock_contention_retry\",\n                        resource_id=resource_id,\n                        attempt=attempt,\n                        backoff_seconds=backoff,\n                    )\n                    time.sleep(backoff)\n                else:\n                    raise DistributedLockError(\n                        resource_id=resource_id,\n                        message=(\n                            f\"DynamoDB error acquiring lock: \"\n                            f\"{exc.response['Error']['Message']}\"\n                        ),\n                    ) from exc\n\n        raise DistributedLockError(\n            resource_id=resource_id,\n            message=\"Unexpected lock acquisition failure\",\n        )\n\n    def release(self, resource_id: str, owner_id: str) -> bool:\n        """Release a distributed lock.\n\n        Only the owner that acquired the lock can release it.\n\n        Args:\n            resource_id: Unique identifier for the locked resource.\n            owner_id: The owner_id returned from acquire().\n\n        Returns:\n            True if the lock was successfully released, False if the lock\n            was not held by this owner (e.g., already expired).\n        """\n        try:\n            self.table.delete_item(\n                Key={\"resource_id\": resource_id},\n                ConditionExpression=\"owner_id = :owner\",\n                ExpressionAttributeValues={\":owner\": owner_id},\n            )\n            logger.info(\n                \"lock_released\",\n                resource_id=resource_id,\n                owner_id=owner_id,\n            )\n            return True\n\n        except ClientError as exc:\n            if (\n                exc.response[\"Error\"][\"Code\"]\n                == \"ConditionalCheckFailedException\"\n            ):\n                logger.warning(\n                    \"lock_release_not_owner\",\n                    resource_id=resource_id,\n                    owner_id=owner_id,\n                )\n                return False\n            raise DistributedLockError(\n                resource_id=resource_id,\n                message=(\n                    f\"DynamoDB error releasing lock: \"\n                    f\"{exc.response['Error']['Message']}\"\n                ),\n            ) from exc\n