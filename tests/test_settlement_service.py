"""\nModule: test_settlement_service\nDescription: Regression tests for BetSettlementService including\n    concurrent settlement scenario that reproduces the duplicate-payout\n    bug reported in SCRUM-2.\nAuthor: AI Virtual Engineer\nDate: 2026-08-26\nJira: SCRUM-2\n"""\n\nimport threading\nimport time\nfrom decimal import Decimal\nfrom typing import Dict, List, Optional\nfrom unittest.mock import MagicMock, patch\n\nimport pytest\n\nfrom exceptions import (\n    BetNotFoundError,\n    DistributedLockError,\n    PayoutProcessingError,\n)\nfrom models import Bet, BetStatus, PayoutResult\nfrom settlement_service import BetSettlementService\n\n\nclass FakeBetRepository:\n    """In-memory bet repository for testing.\n\n    Thread-safe implementation that tracks all operations.\n    """\n\n    def __init__(self, bets: Optional[Dict[str, Bet]] = None) -> None:\n        """Initialize with optional pre-loaded bets."""\n        self._bets: Dict[str, Bet] = bets or {}\n        self._lock = threading.Lock()\n        self.update_count: int = 0\n\n    def get_bet(self, bet_id: str) -> Optional[Bet]:\n        """Retrieve a bet by ID."""\n        with self._lock:\n            return self._bets.get(bet_id)\n\n    def update_bet(self, bet: Bet) -> None:\n        """Update a bet record."""\n        with self._lock:\n            self._bets[bet.bet_id] = bet\n            self.update_count += 1\n\n\nclass FakePayoutGateway:\n    """In-memory payout gateway for testing.\n\n    Tracks all payouts issued for assertion.\n    """\n\n    def __init__(self) -> None:\n        """Initialize the fake payout gateway."""\n        self._payouts: List[Dict] = []\n        self._lock = threading.Lock()\n\n    def process_payout(\n        self,\n        customer_id: str,\n        amount: Decimal,\n        bet_id: str,\n    ) -> None:\n        """Record a payout."""\n        with self._lock:\n            self._payouts.append({\n                \"customer_id\": customer_id,\n                \"amount\": amount,\n                \"bet_id\": bet_id,\n            })\n\n    @property\n    def payout_count(self) -> int:\n        """Return total number of payouts issued."""\n        with self._lock:\n            return len(self._payouts)\n\n    @property\n    def total_paid(self) -> Decimal:\n        """Return total amount paid out."""\n        with self._lock:\n            return sum(p[\"amount\"] for p in self._payouts)\n\n\nclass TestSettleBetBasic:\n    """Basic settlement tests for happy path and edge cases."""\n\n    def _make_pending_bet(self, bet_id: str = \"bet-001\") -> Bet:\n        """Create a standard pending bet for testing."""\n        return Bet(\n            bet_id=bet_id,\n            customer_id=\"cust-123\",\n            match_id=\"match-456\",\n            selection=\"home_win\",\n            stake=Decimal(\"50.00\"),\n            odds=Decimal(\"2.50\"),\n            status=BetStatus.PENDING,\n            placed_at=int(time.time()) - 3600,\n        )\n\n    def test_settle_winning_bet_issues_payout(self) -> None:\n        """Winning bet should trigger exactly one payout."""\n        bet = self._make_pending_bet()\n        repo = FakeBetRepository({\"bet-001\": bet})\n        gateway = FakePayoutGateway()\n        lock = MagicMock()\n        lock.acquire.return_value = \"owner-1\"\n\n        service = BetSettlementService(\n            bet_repository=repo,\n            payout_gateway=gateway,\n            lock=lock,\n        )\n\n        result = service.settle_bet(\n            bet_id=\"bet-001\",\n            match_result=\"home_win\",\n            event_source=\"final_whistle\",\n        )\n\n        assert result.status == \"settled_won\"\n        assert result.payout_amount == Decimal(\"125.00\")\n        assert gateway.payout_count == 1\n        assert bet.status == BetStatus.SETTLED_WON\n        lock.acquire.assert_called_once()\n        lock.release.assert_called_once()\n\n    def test_settle_losing_bet_no_payout(self) -> None:\n        """Losing bet should not trigger a payout."""\n        bet = self._make_pending_bet()\n        repo = FakeBetRepository({\"bet-001\": bet})\n        gateway = FakePayoutGateway()\n        lock = MagicMock()\n        lock.acquire.return_value = \"owner-1\"\n\n        service = BetSettlementService(\n            bet_repository=repo,\n            payout_gateway=gateway,\n            lock=lock,\n        )\n\n        result = service.settle_bet(\n            bet_id=\"bet-001\",\n            match_result=\"away_win\",\n            event_source=\"final_whistle\",\n        )\n\n        assert result.status == \"settled_lost\"\n        assert result.payout_amount == Decimal(\"0\")\n        assert gateway.payout_count == 0\n        assert bet.status == BetStatus.SETTLED_LOST\n\n    def test_settle_already_settled_bet_is_skipped(self) -> None:\n        """Already-settled bet should be skipped (idempotency)."""\n        bet = self._make_pending_bet()\n        bet.status = BetStatus.SETTLED_WON\n        repo = FakeBetRepository({\"bet-001\": bet})\n        gateway = FakePayoutGateway()\n        lock = MagicMock()\n        lock.acquire.return_value = \"owner-1\"\n\n        service = BetSettlementService(\n            bet_repository=repo,\n            payout_gateway=gateway,\n            lock=lock,\n        )\n\n        result = service.settle_bet(\n            bet_id=\"bet-001\",\n            match_result=\"home_win\",\n            event_source=\"var_correction\",\n        )\n\n        assert result.status == \"skipped\"\n        assert result.reason == \"already_processed\"\n        assert gateway.payout_count == 0\n\n    def test_settle_nonexistent_bet_raises_error(self) -> None:\n        """Settling a non-existent bet should raise BetNotFoundError."""\n        repo = FakeBetRepository({})\n        gateway = FakePayoutGateway()\n        lock = MagicMock()\n        lock.acquire.return_value = \"owner-1\"\n\n        service = BetSettlementService(\n            bet_repository=repo,\n            payout_gateway=gateway,\n            lock=lock,\n        )\n\n        with pytest.raises(BetNotFoundError):\n            service.settle_bet(\n                bet_id=\"nonexistent\",\n                match_result=\"home_win\",\n            )\n\n    def test_payout_failure_raises_processing_error(self) -> None:\n        """Payout gateway failure should raise PayoutProcessingError."""\n        bet = self._make_pending_bet()\n        repo = FakeBetRepository({\"bet-001\": bet})\n        gateway = MagicMock()\n        gateway.process_payout.side_effect = RuntimeError(\"timeout\")\n        lock = MagicMock()\n        lock.acquire.return_value = \"owner-1\"\n\n        service = BetSettlementService(\n            bet_repository=repo,\n            payout_gateway=gateway,\n            lock=lock,\n        )\n\n        with pytest.raises(PayoutProcessingError):\n            service.settle_bet(\n                bet_id=\"bet-001\",\n                match_result=\"home_win\",\n            )\n\n\nclass TestConcurrentSettlement:\n    """Tests that reproduce the SCRUM-2 race condition scenario.\n\n    Verifies that with the distributed lock in place, concurrent\n    settlement events for the same bet result in exactly one payout.\n    """\n\n    def test_concurrent_events_single_payout(self) -> None:\n        """Two simultaneous settlement events must produce only one payout.\n\n        This test directly reproduces the reported bug: final whistle\n        and VAR correction events arrive within <500ms for the same bet.\n        Without the lock, both would see status=pending and both would\n        issue a payout. With the lock, only one succeeds.\n        """\n        bet = Bet(\n            bet_id=\"bet-race-001\",\n            customer_id=\"cust-789\",\n            match_id=\"match-101\",\n            selection=\"home_win\",\n            stake=Decimal(\"100.00\"),\n            odds=Decimal(\"3.00\"),\n            status=BetStatus.PENDING,\n            placed_at=int(time.time()) - 7200,\n        )\n        repo = FakeBetRepository({\"bet-race-001\": bet})\n        gateway = FakePayoutGateway()\n\n        # Use a real threading lock to simulate DynamoDB lock behavior\n        real_lock = threading.Lock()\n\n        class ThreadSafeLock:\n            """Simulates distributed lock using threading.Lock."""\n\n            def acquire(\n                self, resource_id: str, **kwargs\n            ) -> str:\n                acquired = real_lock.acquire(timeout=5)\n                if not acquired:\n                    raise DistributedLockError(\n                        resource_id=resource_id,\n                        message=\"Lock timeout\",\n                    )\n                return \"thread-owner\"\n\n            def release(\n                self, resource_id: str, owner_id: str\n            ) -> bool:\n                try:\n                    real_lock.release()\n                    return True\n                except RuntimeError:\n                    return False\n\n        service = BetSettlementService(\n            bet_repository=repo,\n            payout_gateway=gateway,\n            lock=ThreadSafeLock(),\n        )\n\n        results: List[PayoutResult] = []\n        errors: List[Exception] = []\n\n        def settle_event(event_source: str) -> None:\n            try:\n                result = service.settle_bet(\n                    bet_id=\"bet-race-001\",\n                    match_result=\"home_win\",\n                    event_source=event_source,\n                )\n                results.append(result)\n            except Exception as exc:\n                errors.append(exc)\n\n        # Launch two concurrent settlement events\n        t1 = threading.Thread(\n            target=settle_event,\n            args=(\"final_whistle\",),\n        )\n        t2 = threading.Thread(\n            target=settle_event,\n            args=(\"var_correction\",),\n        )\n\n        t1.start()\n        t2.start()\n        t1.join(timeout=10)\n        t2.join(timeout=10)\n\n        # Assertions: exactly one payout, no errors\n        assert len(errors) == 0, f\"Unexpected errors: {errors}\"\n        assert gateway.payout_count == 1, (\n            f\"Expected exactly 1 payout, got {gateway.payout_count}\"\n        )\n        assert gateway.total_paid == Decimal(\"300.00\")\n\n        # One result should be \"settled_won\", the other \"skipped\"\n        statuses = sorted([r.status for r in results])\n        assert statuses == [\"settled_won\", \"skipped\"], (\n            f\"Expected one win and one skip, got {statuses}\"\n        )\n