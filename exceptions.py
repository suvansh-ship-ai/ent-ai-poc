"""\nModule: exceptions\nDescription: Custom exception hierarchy for the Entain bet settlement\n    platform. All exceptions inherit from EntainBaseException and include\n    structured error codes for UKGC audit compliance.\nAuthor: AI Virtual Engineer\nDate: 2026-08-26\nJira: SCRUM-2\n"""\n\nfrom decimal import Decimal\nfrom typing import Optional\n\n\nclass EntainBaseException(Exception):\n    """Base exception for all Entain platform errors.\n\n    Attributes:\n        error_code: Machine-readable error code for logging/alerting.\n        message: Human-readable error description.\n    """\n\n    error_code: str = \"ENT-000\"\n\n    def __init__(\n        self,\n        message: str = \"An unexpected error occurred\",\n        error_code: Optional[str] = None,\n    ) -> None:\n        """Initialize the base exception.\n\n        Args:\n            message: Human-readable error description.\n            error_code: Optional override for the default error code.\n        """\n        self.message = message\n        if error_code:\n            self.error_code = error_code\n        super().__init__(f\"[{self.error_code}] {self.message}\")\n\n\nclass DistributedLockError(EntainBaseException):\n    """Raised when a distributed lock cannot be acquired or released.\n\n    Attributes:\n        resource_id: The resource that the lock was requested for.\n    """\n\n    error_code: str = \"ENT-LOCK-001\"\n\n    def __init__(\n        self,\n        resource_id: str,\n        message: str = \"Failed to acquire distributed lock\",\n    ) -> None:\n        """Initialize DistributedLockError.\n\n        Args:\n            resource_id: The resource identifier for the lock.\n            message: Human-readable error description.\n        """\n        self.resource_id = resource_id\n        super().__init__(message=message)\n\n\nclass BetNotFoundError(EntainBaseException):\n    """Raised when a bet cannot be found in the repository.\n\n    Attributes:\n        bet_id: The identifier of the bet that was not found.\n    """\n\n    error_code: str = \"ENT-BET-001\"\n\n    def __init__(\n        self,\n        bet_id: str,\n        message: str = \"Bet not found\",\n    ) -> None:\n        """Initialize BetNotFoundError.\n\n        Args:\n            bet_id: The identifier of the missing bet.\n            message: Human-readable error description.\n        """\n        self.bet_id = bet_id\n        super().__init__(message=message)\n\n\nclass InvalidBetStateError(EntainBaseException):\n    """Raised when a bet is in an invalid state for the operation.\n\n    Attributes:\n        bet_id: The identifier of the bet.\n        current_state: The current state of the bet.\n        expected_state: The expected state for the operation.\n    """\n\n    error_code: str = \"ENT-BET-002\"\n\n    def __init__(\n        self,\n        bet_id: str,\n        current_state: str,\n        expected_state: str,\n        message: str = \"Bet is in an invalid state\",\n    ) -> None:\n        """Initialize InvalidBetStateError.\n\n        Args:\n            bet_id: The identifier of the bet.\n            current_state: The current state of the bet.\n            expected_state: The expected state for the operation.\n            message: Human-readable error description.\n        """\n        self.bet_id = bet_id\n        self.current_state = current_state\n        self.expected_state = expected_state\n        super().__init__(\n            message=(\n                f\"{message}: bet '{bet_id}' is '{current_state}', \"\n                f\"expected '{expected_state}'\"\n            )\n        )\n\n\nclass PayoutProcessingError(EntainBaseException):\n    """Raised when a payout fails to process.\n\n    Attributes:\n        bet_id: The identifier of the bet.\n        amount: The payout amount that failed.\n    """\n\n    error_code: str = \"ENT-PAY-001\"\n\n    def __init__(\n        self,\n        bet_id: str,\n        amount: Optional[Decimal] = None,\n        message: str = \"Payout processing failed\",\n    ) -> None:\n        """Initialize PayoutProcessingError.\n\n        Args:\n            bet_id: The identifier of the bet.\n            amount: The payout amount that failed to process.\n            message: Human-readable error description.\n        """\n        self.bet_id = bet_id\n        self.amount = amount\n        super().__init__(message=message)\n