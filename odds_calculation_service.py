"""\nOddsCalculationService - Live In-Play Football Odds Recalculation.\n\nConsumes match events (goals, red cards, injuries, substitutions) from the\nevent stream, recalculates live odds for the in-play betting market, validates\nodds bounds, detects suspicious movements (UKGC requirement), publishes updated\nodds to the trading platform, and maintains a full audit trail.\n\nEntain Coding Standards:\n    - Python 3.11+, strict type hints throughout\n    - Google-style docstrings\n    - structlog for structured JSON logging\n    - UKGC audit logging (before/after values, timestamp, actor, reason)\n    - Repository/service layer pattern\n    - Custom exceptions from exceptions.py\n    - Retry with exponential backoff (max 3 attempts)\n\nAuthor: Entain AI Software Engineer\nTicket: SCRUM-1\n"""\n\nfrom __future__ import annotations\n\nimport time\nimport uuid\nfrom dataclasses import dataclass, field\nfrom datetime import datetime, timezone\nfrom decimal import Decimal, ROUND_HALF_UP\nfrom enum import Enum\nfrom typing import Any, Protocol\n\nimport structlog\n\nfrom config import settings\nfrom exceptions import (\n    EntainBaseException,\n    ExternalServiceError,\n    ValidationError,\n)\n\nlogger = structlog.get_logger(__name__)\n\n\n# ---------------------------------------------------------------------------\n# Constants\n# ---------------------------------------------------------------------------\n\nMIN_ODDS = Decimal("1.01")\nMAX_ODDS = Decimal("100.00")\nSUSPICIOUS_MOVEMENT_THRESHOLD = Decimal("0.20")  # 20%\nSUSPICIOUS_MOVEMENT_WINDOW_SECONDS = 60\nDEFAULT_MARGIN = Decimal("0.05")  # 5% platform margin\nMAX_RETRY_ATTEMPTS = 3\nRETRY_BASE_DELAY_SECONDS = 1.0\n\n\n# ---------------------------------------------------------------------------\n# Enums\n# ---------------------------------------------------------------------------\n\n\nclass MatchEventType(str, Enum):\n    """Supported match event types that trigger odds recalculation."""\n\n    GOAL = "goal"\n    RED_CARD = "red_card"\n    YELLOW_CARD = "yellow_card"\n    INJURY = "injury"\n    SUBSTITUTION = "substitution"\n    PENALTY_AWARDED = "penalty_awarded"\n    VAR_DECISION = "var_decision"\n    HALF_TIME = "half_time"\n    FULL_TIME = "full_time"\n\n\nclass OddsOutcome(str, Enum):\n    """Standard 1X2 match result outcomes."""\n\n    HOME_WIN = "home_win"\n    DRAW = "draw"\n    AWAY_WIN = "away_win"\n\n\n# ---------------------------------------------------------------------------\n# Data Classes\n# ---------------------------------------------------------------------------\n\n\n@dataclass(frozen=True)\nclass MatchEvent:\n    """Represents a single match event from the event stream.\n\n    Attributes:\n        event_id: Unique identifier for the event.\n        match_id: Identifier of the match this event belongs to.\n        event_type: The type of match event.\n        team: Which team the event applies to ('home' or 'away').\n        minute: Match minute when the event occurred.\n        timestamp: UTC timestamp of the event.\n        metadata: Additional event-specific data.\n    """\n\n    event_id: str\n    match_id: str\n    event_type: MatchEventType\n    team: str\n    minute: int\n    timestamp: datetime\n    metadata: dict[str, Any] = field(default_factory=dict)\n\n\n@dataclass\nclass OddsSnapshot:\n    """A point-in-time snapshot of odds for a match.\n\n    Attributes:\n        match_id: Identifier of the match.\n        home_win: Decimal odds for home win.\n        draw: Decimal odds for draw.\n        away_win: Decimal odds for away win.\n        timestamp: When these odds were calculated.\n        version: Monotonically increasing version for optimistic locking.\n    """\n\n    match_id: str\n    home_win: Decimal\n    draw: Decimal\n    away_win: Decimal\n    timestamp: datetime\n    version: int = 0\n\n\n@dataclass(frozen=True)\nclass AuditRecord:\n    """UKGC-compliant audit record for odds changes.\n\n    Attributes:\n        audit_id: Unique identifier for this audit entry.\n        match_id: Match identifier.\n        event_id: The triggering event identifier.\n        actor: System or user that initiated the change.\n        reason: Human-readable reason for the change.\n        before: Odds snapshot before the change.\n        after: Odds snapshot after the change.\n        timestamp: UTC timestamp of the audit entry.\n        suspicious: Whether this change was flagged as suspicious.\n    """\n\n    audit_id: str\n    match_id: str\n    event_id: str\n    actor: str\n    reason: str\n    before: dict[str, str]\n    after: dict[str, str]\n    timestamp: datetime\n    suspicious: bool = False\n\n\n# ---------------------------------------------------------------------------\n# Repository Protocols (Dependency Injection)\n# ---------------------------------------------------------------------------\n\n\nclass OddsRepository(Protocol):\n    """Protocol for odds persistence operations."""\n\n    def get_current_odds(self, match_id: str) -> OddsSnapshot | None:\n        """Retrieve the current odds for a match."""\n        ...\n\n    def save_odds(self, snapshot: OddsSnapshot) -> None:\n        """Persist an odds snapshot."""\n        ...\n\n\nclass AuditLogRepository(Protocol):\n    """Protocol for UKGC audit log persistence."""\n\n    def save_audit_record(self, record: AuditRecord) -> None:\n        """Persist an audit record."""\n        ...\n\n\nclass TradingPlatformPublisher(Protocol):\n    """Protocol for publishing odds to the trading platform."""\n\n    def publish_odds(self, snapshot: OddsSnapshot) -> None:\n        """Publish updated odds to the trading platform."""\n        ...\n\n\nclass AlertService(Protocol):\n    """Protocol for sending alerts on suspicious activity."""\n\n    def send_suspicious_movement_alert(\n        self, match_id: str, details: dict[str, Any]\n    ) -> None:\n        """Send an alert for suspicious odds movement."""\n        ...\n\n\n# ---------------------------------------------------------------------------\n# Event Impact Weightings\n# ---------------------------------------------------------------------------\n\n# Maps event types to their impact on the implied probability of the\n# affected team winning. Positive values increase the team's win probability.\nEVENT_IMPACT_WEIGHTS: dict[MatchEventType, Decimal] = {\n    MatchEventType.GOAL: Decimal("0.15"),\n    MatchEventType.RED_CARD: Decimal("-0.08"),\n    MatchEventType.YELLOW_CARD: Decimal("-0.01"),\n    MatchEventType.INJURY: Decimal("-0.03"),\n    MatchEventType.SUBSTITUTION: Decimal("0.00"),\n    MatchEventType.PENALTY_AWARDED: Decimal("0.10"),\n    MatchEventType.VAR_DECISION: Decimal("0.00"),\n    MatchEventType.HALF_TIME: Decimal("0.00"),\n    MatchEventType.FULL_TIME: Decimal("0.00"),\n}\n\n\n# ---------------------------------------------------------------------------\n# Service Implementation\n# ---------------------------------------------------------------------------\n\n\nclass OddsCalculationService:\n    """Service for recalculating live in-play football match odds.\n\n    This service consumes match events, recalculates odds using implied\n    probability adjustments, applies platform margin, validates bounds,\n    detects suspicious movements per UKGC requirements, persists results,\n    and publishes to the trading platform.\n\n    Args:\n        odds_repository: Repository for odds persistence.\n        audit_repository: Repository for UKGC audit logs.\n        trading_publisher: Publisher for the trading platform.\n        alert_service: Service for suspicious movement alerts.\n        margin: Platform margin to apply (default 5%).\n\n    Example:\n        >>> service = OddsCalculationService(\n        ...     odds_repository=odds_repo,\n        ...     audit_repository=audit_repo,\n        ...     trading_publisher=publisher,\n        ...     alert_service=alerter,\n        ... )\n        >>> service.process_event(match_event)\n    """\n\n    def __init__(\n        self,\n        odds_repository: OddsRepository,\n        audit_repository: AuditLogRepository,\n        trading_publisher: TradingPlatformPublisher,\n        alert_service: AlertService,\n        margin: Decimal = DEFAULT_MARGIN,\n    ) -> None:\n        self._odds_repo = odds_repository\n        self._audit_repo = audit_repository\n        self._trading_publisher = trading_publisher\n        self._alert_service = alert_service\n        self._margin = margin\n        self._log = logger.bind(service="OddsCalculationService")\n\n    # ------------------------------------------------------------------\n    # Public API\n    # ------------------------------------------------------------------\n\n    def process_event(self, event: MatchEvent) -> OddsSnapshot:\n        """Process a match event and recalculate odds.\n\n        This is the main entry point. It orchestrates the full lifecycle:\n        1. Retrieve current odds\n        2. Calculate new odds based on event impact\n        3. Apply platform margin\n        4. Validate odds bounds\n        5. Detect suspicious movements\n        6. Persist new odds\n        7. Log audit trail\n        8. Publish to trading platform\n\n        Args:\n            event: The match event to process.\n\n        Returns:\n            The newly calculated OddsSnapshot.\n\n        Raises:\n            ValidationError: If the event or resulting odds are invalid.\n            ExternalServiceError: If publishing or persistence fails\n                after retries.\n        """\n        self._log.info(\n            "processing_match_event",\n            event_id=event.event_id,\n            match_id=event.match_id,\n            event_type=event.event_type.value,\n            team=event.team,\n            minute=event.minute,\n        )\n\n        # 1. Retrieve current odds\n        current_odds = self._get_current_odds(event.match_id)\n\n        # 2. Calculate new odds\n        new_odds = self._recalculate_odds(current_odds, event)\n\n        # 3. Apply margin\n        new_odds = self._apply_margin(new_odds)\n\n        # 4. Validate bounds\n        self._validate_odds(new_odds)\n\n        # 5. Detect suspicious movements\n        is_suspicious = self._check_suspicious_movement(\n            current_odds, new_odds, event\n        )\n\n        # 6. Persist new odds (with retry)\n        self._persist_odds_with_retry(new_odds)\n\n        # 7. Log UKGC audit trail\n        self._log_audit(\n            event=event,\n            before=current_odds,\n            after=new_odds,\n            suspicious=is_suspicious,\n        )\n\n        # 8. Publish to trading platform (with retry)\n        self._publish_with_retry(new_odds)\n\n        self._log.info(\n            "event_processed_successfully",\n            event_id=event.event_id,\n            match_id=event.match_id,\n            new_home_win=str(new_odds.home_win),\n            new_draw=str(new_odds.draw),\n            new_away_win=str(new_odds.away_win),\n            suspicious=is_suspicious,\n        )\n\n        return new_odds\n\n    # ------------------------------------------------------------------\n    # Private Methods\n    # ------------------------------------------------------------------\n\n    def _get_current_odds(self, match_id: str) -> OddsSnapshot:\n        """Retrieve current odds or create default baseline.\n\n        Args:\n            match_id: The match identifier.\n\n        Returns:\n            Current odds snapshot or a default baseline.\n        """\n        current = self._odds_repo.get_current_odds(match_id)\n        if current is None:\n            self._log.warning(\n                "no_existing_odds_found_using_baseline",\n                match_id=match_id,\n            )\n            current = OddsSnapshot(\n                match_id=match_id,\n                home_win=Decimal("2.50"),\n                draw=Decimal("3.20"),\n                away_win=Decimal("2.80"),\n                timestamp=datetime.now(timezone.utc),\n                version=0,\n            )\n        return current\n\n    def _recalculate_odds(\n        self, current: OddsSnapshot, event: MatchEvent\n    ) -> OddsSnapshot:\n        """Recalculate odds based on the event impact.\n\n        Uses implied probability model: converts decimal odds to implied\n        probabilities, adjusts based on event impact weights, normalises\n        to ensure probabilities sum to 1, then converts back to decimal odds.\n\n        Args:\n            current: The current odds snapshot.\n            event: The triggering match event.\n\n        Returns:\n            New odds snapshot with recalculated values.\n        """\n        impact = EVENT_IMPACT_WEIGHTS.get(event.event_type, Decimal("0.00"))\n\n        # Convert to implied probabilities\n        home_prob = Decimal("1") / current.home_win\n        draw_prob = Decimal("1") / current.draw\n        away_prob = Decimal("1") / current.away_win\n\n        # Apply impact based on which team the event affects\n        if event.team == "home":\n            home_prob += impact\n            away_prob -= impact * Decimal("0.6")\n            draw_prob -= impact * Decimal("0.4")\n        elif event.team == "away":\n            away_prob += impact\n            home_prob -= impact * Decimal("0.6")\n            draw_prob -= impact * Decimal("0.4")\n        else:\n            # Neutral events (e.g., half_time) — no adjustment\n            pass\n\n        # Ensure no probability goes below a minimum threshold\n        min_prob = Decimal("0.01")\n        home_prob = max(home_prob, min_prob)\n        draw_prob = max(draw_prob, min_prob)\n        away_prob = max(away_prob, min_prob)\n\n        # Normalise probabilities to sum to 1\n        total = home_prob + draw_prob + away_prob\n        home_prob = (home_prob / total).quantize(\n            Decimal("0.0001"), rounding=ROUND_HALF_UP\n        )\n        draw_prob = (draw_prob / total).quantize(\n            Decimal("0.0001"), rounding=ROUND_HALF_UP\n        )\n        away_prob = (away_prob / total).quantize(\n            Decimal("0.0001"), rounding=ROUND_HALF_UP\n        )\n\n        # Convert back to decimal odds\n        new_home = (Decimal("1") / home_prob).quantize(\n            Decimal("0.01"), rounding=ROUND_HALF_UP\n        )\n        new_draw = (Decimal("1") / draw_prob).quantize(\n            Decimal("0.01"), rounding=ROUND_HALF_UP\n        )\n        new_away = (Decimal("1") / away_prob).quantize(\n            Decimal("0.01"), rounding=ROUND_HALF_UP\n        )\n\n        return OddsSnapshot(\n            match_id=current.match_id,\n            home_win=new_home,\n            draw=new_draw,\n            away_win=new_away,\n            timestamp=datetime.now(timezone.utc),\n            version=current.version + 1,\n        )\n\n    def _apply_margin(self, snapshot: OddsSnapshot) -> OddsSnapshot:\n        """Apply platform margin to odds.\n\n        Reduces each odds value slightly to build in the bookmaker margin.\n\n        Args:\n            snapshot: The raw odds snapshot.\n\n        Returns:\n            Odds snapshot with margin applied.\n        """\n        margin_factor = Decimal("1") - self._margin\n\n        return OddsSnapshot(\n            match_id=snapshot.match_id,\n            home_win=(snapshot.home_win * margin_factor).quantize(\n                Decimal("0.01"), rounding=ROUND_HALF_UP\n            ),\n            draw=(snapshot.draw * margin_factor).quantize(\n                Decimal("0.01"), rounding=ROUND_HALF_UP\n            ),\n            away_win=(snapshot.away_win * margin_factor).quantize(\n                Decimal("0.01"), rounding=ROUND_HALF_UP\n            ),\n            timestamp=snapshot.timestamp,\n            version=snapshot.version,\n        )\n\n    def _validate_odds(self, snapshot: OddsSnapshot) -> None:\n        """Validate that all odds are within acceptable bounds.\n\n        Args:\n            snapshot: The odds snapshot to validate.\n\n        Raises:\n            ValidationError: If any odds value is outside [MIN_ODDS, MAX_ODDS].\n        """\n        for outcome, value in [\n            (OddsOutcome.HOME_WIN, snapshot.home_win),\n            (OddsOutcome.DRAW, snapshot.draw),\n            (OddsOutcome.AWAY_WIN, snapshot.away_win),\n        ]:\n            if value < MIN_ODDS or value > MAX_ODDS:\n                self._log.error(\n                    "odds_validation_failed",\n                    match_id=snapshot.match_id,\n                    outcome=outcome.value,\n                    value=str(value),\n                    min_allowed=str(MIN_ODDS),\n                    max_allowed=str(MAX_ODDS),\n                )\n                raise ValidationError(\n                    f"Odds for {outcome.value} ({value}) outside allowed "\n                    f"range [{MIN_ODDS}, {MAX_ODDS}]"\n                )\n\n    def _check_suspicious_movement(\n        self,\n        before: OddsSnapshot,\n        after: OddsSnapshot,\n        event: MatchEvent,\n    ) -> bool:\n        """Detect suspicious odds movements per UKGC requirements.\n\n        A movement is suspicious if any outcome's odds change by more than\n        20% within a 60-second window.\n\n        Args:\n            before: Odds before the event.\n            after: Odds after the event.\n            event: The triggering event.\n\n        Returns:\n            True if the movement is flagged as suspicious.\n        """\n        time_delta = (\n            after.timestamp - before.timestamp\n        ).total_seconds()\n\n        if time_delta > SUSPICIOUS_MOVEMENT_WINDOW_SECONDS:\n            return False\n\n        movements: dict[str, Decimal] = {}\n        for outcome, old_val, new_val in [\n            (OddsOutcome.HOME_WIN, before.home_win, after.home_win),\n            (OddsOutcome.DRAW, before.draw, after.draw),\n            (OddsOutcome.AWAY_WIN, before.away_win, after.away_win),\n        ]:\n            if old_val > 0:\n                pct_change = abs(new_val - old_val) / old_val\n                movements[outcome.value] = pct_change\n\n        is_suspicious = any(\n            v > SUSPICIOUS_MOVEMENT_THRESHOLD for v in movements.values()\n        )\n\n        if is_suspicious:\n            self._log.warning(\n                "suspicious_odds_movement_detected",\n                match_id=after.match_id,\n                event_id=event.event_id,\n                movements={k: str(v) for k, v in movements.items()},\n                time_delta_seconds=time_delta,\n                threshold=str(SUSPICIOUS_MOVEMENT_THRESHOLD),\n            )\n            self._alert_service.send_suspicious_movement_alert(\n                match_id=after.match_id,\n                details={\n                    "event_id": event.event_id,\n                    "event_type": event.event_type.value,\n                    "movements": {k: str(v) for k, v in movements.items()},\n                    "time_delta_seconds": time_delta,\n                },\n            )\n\n        return is_suspicious\n\n    def _persist_odds_with_retry(self, snapshot: OddsSnapshot) -> None:\n        """Persist odds with retry and exponential backoff.\n\n        Args:\n            snapshot: The odds snapshot to persist.\n\n        Raises:\n            ExternalServiceError: If persistence fails after max retries.\n        """\n        self._execute_with_retry(\n            operation_name="persist_odds",\n            func=lambda: self._odds_repo.save_odds(snapshot),\n            match_id=snapshot.match_id,\n        )\n\n    def _publish_with_retry(self, snapshot: OddsSnapshot) -> None:\n        """Publish odds to trading platform with retry and exponential backoff.\n\n        Args:\n            snapshot: The odds snapshot to publish.\n\n        Raises:\n            ExternalServiceError: If publishing fails after max retries.\n        """\n        self._execute_with_retry(\n            operation_name="publish_odds",\n            func=lambda: self._trading_publisher.publish_odds(snapshot),\n            match_id=snapshot.match_id,\n        )\n\n    def _execute_with_retry(\n        self,\n        operation_name: str,\n        func: Any,\n        match_id: str,\n    ) -> None:\n        """Execute a function with retry and exponential backoff.\n\n        Args:\n            operation_name: Name of the operation for logging.\n            func: Callable to execute.\n            match_id: Match identifier for logging context.\n\n        Raises:\n            ExternalServiceError: If the operation fails after max retries.\n        """\n        last_exception: Exception | None = None\n\n        for attempt in range(1, MAX_RETRY_ATTEMPTS + 1):\n            try:\n                func()\n                self._log.debug(\n                    f"{operation_name}_succeeded",\n                    match_id=match_id,\n                    attempt=attempt,\n                )\n                return\n            except Exception as exc:\n                last_exception = exc\n                delay = RETRY_BASE_DELAY_SECONDS * (2 ** (attempt - 1))\n                self._log.warning(\n                    f"{operation_name}_failed_retrying",\n                    match_id=match_id,\n                    attempt=attempt,\n                    max_attempts=MAX_RETRY_ATTEMPTS,\n                    delay_seconds=delay,\n                    error=str(exc),\n                )\n                if attempt < MAX_RETRY_ATTEMPTS:\n                    time.sleep(delay)\n\n        self._log.error(\n            f"{operation_name}_failed_permanently",\n            match_id=match_id,\n            attempts_exhausted=MAX_RETRY_ATTEMPTS,\n            error=str(last_exception),\n        )\n        raise ExternalServiceError(\n            f"{operation_name} failed after {MAX_RETRY_ATTEMPTS} attempts: "\n            f"{last_exception}"\n        )\n\n    def _log_audit(\n        self,\n        event: MatchEvent,\n        before: OddsSnapshot,\n        after: OddsSnapshot,\n        suspicious: bool,\n    ) -> None:\n        """Create and persist a UKGC-compliant audit record.\n\n        Records before/after odds values, the triggering event, timestamp,\n        actor, and reason for the change.\n\n        Args:\n            event: The triggering match event.\n            before: Odds before the change.\n            after: Odds after the change.\n            suspicious: Whether the movement was flagged as suspicious.\n        """\n        record = AuditRecord(\n            audit_id=str(uuid.uuid4()),\n            match_id=event.match_id,\n            event_id=event.event_id,\n            actor="OddsCalculationService",\n            reason=(\n                f"Odds recalculated due to {event.event_type.value} "\n                f"event at minute {event.minute} ({event.team})"\n            ),\n            before={\n                "home_win": str(before.home_win),\n                "draw": str(before.draw),\n                "away_win": str(before.away_win),\n                "version": str(before.version),\n            },\n            after={\n                "home_win": str(after.home_win),\n                "draw": str(after.draw),\n                "away_win": str(after.away_win),\n                "version": str(after.version),\n            },\n            timestamp=datetime.now(timezone.utc),\n            suspicious=suspicious,\n        )\n\n        # Persist audit record\n        self._audit_repo.save_audit_record(record)\n\n        # Structured log for UKGC compliance\n        self._log.info(\n            "ukgc_audit_log",\n            audit_id=record.audit_id,\n            match_id=record.match_id,\n            event_id=record.event_id,\n            actor=record.actor,\n            reason=record.reason,\n            before=record.before,\n            after=record.after,\n            suspicious=record.suspicious,\n            timestamp=record.timestamp.isoformat(),\n        )\n