"""\nModule: config\nDescription: Configuration constants for the Entain bet settlement\n    platform. All environment-specific values are loaded from\n    environment variables with sensible defaults.\nAuthor: AI Virtual Engineer\nDate: 2026-08-26\nJira: SCRUM-2\n"""\n\nimport os\n\n\nclass SettlementConfig:\n    """Configuration for the bet settlement service.\n\n    All values are read from environment variables with defaults\n    suitable for local development. Never hardcode secrets here.\n    """\n\n    # DynamoDB table names\n    LOCK_TABLE_NAME: str = os.environ.get(\n        \"LOCK_TABLE_NAME\", \"bet-settlement-locks\"\n    )\n    BET_TABLE_NAME: str = os.environ.get(\n        \"BET_TABLE_NAME\", \"bets\"\n    )\n\n    # Lock configuration\n    LOCK_TTL_SECONDS: int = int(\n        os.environ.get(\"LOCK_TTL_SECONDS\", \"30\")\n    )\n    LOCK_MAX_RETRIES: int = int(\n        os.environ.get(\"LOCK_MAX_RETRIES\", \"3\")\n    )\n\n    # AWS region\n    AWS_REGION: str = os.environ.get(\n        \"AWS_REGION\", \"eu-west-1\"\n    )\n\n    # Logging level\n    LOG_LEVEL: str = os.environ.get(\n        \"LOG_LEVEL\", \"INFO\"\n    )\n