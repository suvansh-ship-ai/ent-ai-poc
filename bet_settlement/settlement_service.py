"""\nBet Settlement Service \u2014 race-condition-free implementation.\n\nThis module provides the core settlement logic with:\n- DynamoDB distributed locking (per-bet) to prevent concurrent settlement\n- Defence-in-depth idempotency check inside the lock\n- UKGC-compliant structured audit logging\n- Clean separation of concerns\n\nArchitecture:\n    Event \u2192 settle_bet() \u2192 [acquire lock] \u2192 [re-read bet] \u2192 [idempotency check]\n        \u2192 [process payout] \u2192 [update status] \u2192 [release lock]\n"""\n\nfrom __future__ import annotations\n\nimport time\nfrom dataclasses import dataclass\nfrom decimal import Decimal\nfrom typing import Optional, Protocol\n\nimport structlog\n\nfrom .distributed_lock import DistributedLock\nfrom .exceptions import (\n    BetNotFoundError,\n    DistributedLockError,\n    InvalidBetStateError,\n    PayoutProcessingError,\n)\n\nlogger = structlog.get_logger(__name__)\n\n\n# --- Domain Models ---\n\n\n@dataclass\nclass Bet:\n    """Represents a customer bet."""\n\n    bet_id: str\n    customer_id: str\n    match_id: str\n    stake: Decimal\n    odds: Decimal\n    status: str  # \"pending\", \"settled\", \"voided\"\n    outcome: Optional[str] = None  # \"won\", \"lost\"\n\n\n@dataclass\nclass SettlementResult:\n    """Result of a settlement operation."""\n\n    bet_id: str\n    status: str\n    payout_amount: Decimal\n    settled: bool\n    message: str\n\n\n# --- Port Interfaces (Dependency Inversion) ---\n\n\nclass BetRepository(Protocol):\n    """Interface for bet data access."""\n\n    def get_bet(self, bet_id: str) -> Optional[Bet]:\n        ...\n\n    def update_bet_status(self, bet_id: str, status: str, outcome: str) -> None:\n        ...\n\n\nclass PayoutGateway(Protocol):\n    """Interface for payout processing."""\n\n    def process_payout(self, customer_id: str, amount: Decimal, bet_id: str) -> bool:\n        ...\n\n\n# --- Service Implementation ---\n\n\nclass BetSettlementService:\n    """\n    Settles bets with distributed locking to prevent duplicate payouts.\n\n    The service acquires a per-bet DynamoDB lock before processing,\n    ensuring that even if multiple settlement events arrive concurrently\n    (e.g., final whistle + VAR correction within <500ms), only one\n    settlement is processed.\n    """\n\n    def __init__(\n        self,\n        bet_repository: BetRepository,\n        payout_gateway: PayoutGateway,\n        lock_table: str = "bet-settlement-locks",\n    ) -> None:\n        self._bet_repository = bet_repository\n        self._payout_gateway = payout_gateway\n        self._lock_table = lock_table\n\n    def settle_bet(self, bet_id: str, outcome: str) -> SettlementResult:\n        """\n        Settle a bet with race-condition protection.\n\n        Acquires a distributed lock keyed on the bet_id, then re-reads\n        the bet status to perform a defence-in-depth idempotency check\n        before processing the payout.\n\n        Args:\n            bet_id: Unique identifier of the bet to settle.\n            outcome: Settlement outcome \u2014 \"won\" or \"lost\".\n\n        Returns:\n            SettlementResult with details of the operation.\n\n        Raises:\n            DistributedLockError: If the lock cannot be acquired.\n            BetNotFoundError: If the bet does not exist.\n            PayoutProcessingError: If payout fails after lock acquisition.\n        """\n        lock = DistributedLock(\n            resource_id=f"bet-settlement:{bet_id}",\n            table_name=self._lock_table,\n        )\n\n        logger.info(\n            "settlement_initiated",\n            bet_id=bet_id,\n            outcome=outcome,\n            timestamp=time.time(),\n        )\n\n        with lock:\n            # Re-read bet inside the lock (defence-in-depth)\n            bet = self._bet_repository.get_bet(bet_id)\n\n            if bet is None:\n                raise BetNotFoundError(bet_id=bet_id)\n\n            # Idempotency check \u2014 if already settled, return early\n            if bet.status == "settled":\n                logger.info(\n                    "settlement_already_processed",\n                    bet_id=bet_id,\n                    current_status=bet.status,\n                )\n                return SettlementResult(\n                    bet_id=bet_id,\n                    status="already_settled",\n                    payout_amount=Decimal("0"),\n                    settled=False,\n                    message="Bet was already settled (idempotency guard).",\n                )\n\n            if bet.status != "pending":\n                raise InvalidBetStateError(\n                    bet_id=bet_id,\n                    current_state=bet.status,\n                    expected_state="pending",\n                )\n\n            # UKGC audit log \u2014 BEFORE payout\n            logger.info(\n                "audit_settlement_before",\n                bet_id=bet_id,\n                customer_id=bet.customer_id,\n                match_id=bet.match_id,\n                stake=str(bet.stake),\n                odds=str(bet.odds),\n                previous_status=bet.status,\n                intended_outcome=outcome,\n                timestamp=time.time(),\n                actor="settlement_engine",\n            )\n\n            # Calculate payout\n            payout_amount = Decimal("0")\n            if outcome == "won":\n                payout_amount = bet.stake * bet.odds\n\n            # Process payout (only if won)\n            if payout_amount > 0:\n                success = self._payout_gateway.process_payout(\n                    customer_id=bet.customer_id,\n                    amount=payout_amount,\n                    bet_id=bet_id,\n                )\n                if not success:\n                    raise PayoutProcessingError(\n                        bet_id=bet_id,\n                        reason="Payout gateway returned failure",\n                    )\n\n            # Update bet status\n            self._bet_repository.update_bet_status(\n                bet_id=bet_id,\n                status="settled",\n                outcome=outcome,\n            )\n\n            # UKGC audit log \u2014 AFTER payout\n            logger.info(\n                "audit_settlement_after",\n                bet_id=bet_id,\n                customer_id=bet.customer_id,\n                match_id=bet.match_id,\n                payout_amount=str(payout_amount),\n                new_status="settled",\n                outcome=outcome,\n                timestamp=time.time(),\n                actor="settlement_engine",\n            )\n\n            return SettlementResult(\n                bet_id=bet_id,\n                status="settled",\n                payout_amount=payout_amount,\n                settled=True,\n                message=f"Bet settled successfully. Outcome: {outcome}. "\n                f"Payout: \u00a3{payout_amount}",\n            )\n