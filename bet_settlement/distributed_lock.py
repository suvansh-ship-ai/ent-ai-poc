"""\nDistributed lock implementation using DynamoDB conditional writes.\n\nProvides a per-resource lock mechanism to prevent concurrent processing\nof the same entity (e.g., bet settlement). Uses DynamoDB's conditional\nPutItem with TTL-based expiry for automatic cleanup of stale locks.\n\nStandards:\n- PEP 8 compliant\n- Type-annotated\n- Structured logging via structlog\n- Exponential backoff retry (max 3 attempts)\n"""\n\nfrom __future__ import annotations\n\nimport time\nimport uuid\nfrom typing import Optional\n\nimport boto3\nimport structlog\nfrom botocore.exceptions import ClientError\n\nlogger = structlog.get_logger(__name__)\n\n# Default configuration\nDEFAULT_LOCK_TABLE = "bet-settlement-locks"\nDEFAULT_TTL_SECONDS = 30\nMAX_RETRY_ATTEMPTS = 3\nBASE_BACKOFF_MS = 50\n\n\nclass DistributedLockError(Exception):\n    """Raised when a distributed lock cannot be acquired after all retries."""\n\n    def __init__(self, resource_id: str, attempts: int) -> None:\n        self.resource_id = resource_id\n        self.attempts = attempts\n        super().__init__(\n            f"Failed to acquire lock for resource '{resource_id}' "\n            f"after {attempts} attempts"\n        )\n\n\nclass DistributedLock:\n    """\n    DynamoDB-backed distributed lock with TTL expiry.\n\n    Usage:\n        lock = DistributedLock(resource_id="bet-12345")\n        with lock:\n            # Critical section - only one process can be here per resource\n            process_settlement(bet)\n\n    The lock uses DynamoDB conditional writes to guarantee mutual exclusion.\n    A TTL attribute ensures stale locks are automatically cleaned up if the\n    holder crashes without releasing.\n    """\n\n    def __init__(\n        self,\n        resource_id: str,\n        table_name: str = DEFAULT_LOCK_TABLE,\n        ttl_seconds: int = DEFAULT_TTL_SECONDS,\n        dynamodb_client: Optional[object] = None,\n    ) -> None:\n        self.resource_id = resource_id\n        self.table_name = table_name\n        self.ttl_seconds = ttl_seconds\n        self.lock_token = str(uuid.uuid4())\n        self._client = dynamodb_client or boto3.client("dynamodb")\n        self._acquired = False\n\n    def acquire(self) -> bool:\n        """\n        Attempt to acquire the lock with exponential backoff retry.\n\n        Returns:\n            True if the lock was successfully acquired.\n\n        Raises:\n            DistributedLockError: If the lock cannot be acquired after\n                MAX_RETRY_ATTEMPTS attempts.\n        """\n        for attempt in range(1, MAX_RETRY_ATTEMPTS + 1):\n            try:\n                now = int(time.time())\n                expiry = now + self.ttl_seconds\n\n                self._client.put_item(\n                    TableName=self.table_name,\n                    Item={\n                        "resource_id": {"S": self.resource_id},\n                        "lock_token": {"S": self.lock_token},\n                        "acquired_at": {"N": str(now)},\n                        "ttl": {"N": str(expiry)},\n                    },\n                    ConditionExpression=(\n                        "attribute_not_exists(resource_id) OR #ttl < :now"\n                    ),\n                    ExpressionAttributeNames={"#ttl": "ttl"},\n                    ExpressionAttributeValues={":now": {"N": str(now)}},\n                )\n\n                self._acquired = True\n                logger.info(\n                    "lock_acquired",\n                    resource_id=self.resource_id,\n                    lock_token=self.lock_token,\n                    attempt=attempt,\n                )\n                return True\n\n            except ClientError as e:\n                if e.response["Error"]["Code"] == "ConditionalCheckFailedException":\n                    if attempt < MAX_RETRY_ATTEMPTS:\n                        backoff_ms = BASE_BACKOFF_MS * (2 ** (attempt - 1))\n                        logger.warning(\n                            "lock_contention",\n                            resource_id=self.resource_id,\n                            attempt=attempt,\n                            backoff_ms=backoff_ms,\n                        )\n                        time.sleep(backoff_ms / 1000.0)\n                    else:\n                        logger.error(\n                            "lock_acquisition_failed",\n                            resource_id=self.resource_id,\n                            attempts=MAX_RETRY_ATTEMPTS,\n                        )\n                        raise DistributedLockError(\n                            resource_id=self.resource_id,\n                            attempts=MAX_RETRY_ATTEMPTS,\n                        ) from e\n                else:\n                    raise\n\n        return False  # pragma: no cover\n\n    def release(self) -> None:\n        """\n        Release the lock by deleting the DynamoDB item.\n\n        Only releases if the lock_token matches (prevents releasing\n        a lock that was already expired and re-acquired by another process).\n        """\n        if not self._acquired:\n            return\n\n        try:\n            self._client.delete_item(\n                TableName=self.table_name,\n                Key={"resource_id": {"S": self.resource_id}},\n                ConditionExpression="lock_token = :token",\n                ExpressionAttributeValues={\n                    ":token": {"S": self.lock_token}\n                },\n            )\n            logger.info(\n                "lock_released",\n                resource_id=self.resource_id,\n                lock_token=self.lock_token,\n            )\n        except ClientError as e:\n            if e.response["Error"]["Code"] == "ConditionalCheckFailedException":\n                logger.warning(\n                    "lock_already_expired",\n                    resource_id=self.resource_id,\n                    lock_token=self.lock_token,\n                )\n            else:\n                raise\n        finally:\n            self._acquired = False\n\n    def __enter__(self) -> "DistributedLock":\n        self.acquire()\n        return self\n\n    def __exit__(self, exc_type, exc_val, exc_tb) -> None:\n        self.release()\n