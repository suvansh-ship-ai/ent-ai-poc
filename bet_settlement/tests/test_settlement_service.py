"""\nRegression tests for BetSettlementService.\n\nCovers:\n- Happy path: single bet settlement (won/lost)\n- Idempotency: duplicate settlement returns early without double payout\n- Concurrent settlement: simulates the race condition scenario\n- Error handling: bet not found, invalid state, payout failure\n"""\n\nfrom __future__ import annotations\n\nimport threading\nimport time\nfrom decimal import Decimal\nfrom typing import Optional\nfrom unittest.mock import MagicMock, patch\n\nimport pytest\n\nfrom ..distributed_lock import DistributedLock, DistributedLockError\nfrom ..exceptions import (\n    BetNotFoundError,\n    InvalidBetStateError,\n    PayoutProcessingError,\n)\nfrom ..settlement_service import Bet, BetSettlementService, SettlementResult\n\n\n# --- Test Fixtures ---\n\n\nclass FakeBetRepository:\n    """In-memory bet repository for testing."""\n\n    def __init__(self, bets: Optional[dict[str, Bet]] = None) -> None:\n        self._bets = bets or {}\n        self._lock = threading.Lock()\n\n    def get_bet(self, bet_id: str) -> Optional[Bet]:\n        return self._bets.get(bet_id)\n\n    def update_bet_status(self, bet_id: str, status: str, outcome: str) -> None:\n        with self._lock:\n            if bet_id in self._bets:\n                self._bets[bet_id].status = status\n                self._bets[bet_id].outcome = outcome\n\n\nclass FakePayoutGateway:\n    """Tracks payout calls for assertion."""\n\n    def __init__(self, should_succeed: bool = True) -> None:\n        self.calls: list[dict] = []\n        self._should_succeed = should_succeed\n        self._lock = threading.Lock()\n\n    def process_payout(self, customer_id: str, amount: Decimal, bet_id: str) -> bool:\n        with self._lock:\n            self.calls.append(\n                {"customer_id": customer_id, "amount": amount, "bet_id": bet_id}\n            )\n        return self._should_succeed\n\n\n@pytest.fixture\ndef sample_bet() -> Bet:\n    return Bet(\n        bet_id="bet-001",\n        customer_id="cust-123",\n        match_id="match-456",\n        stake=Decimal("50.00"),\n        odds=Decimal("2.50"),\n        status="pending",\n    )\n\n\n@pytest.fixture\ndef bet_repo(sample_bet: Bet) -> FakeBetRepository:\n    return FakeBetRepository(bets={"bet-001": sample_bet})\n\n\n@pytest.fixture\ndef payout_gateway() -> FakePayoutGateway:\n    return FakePayoutGateway(should_succeed=True)\n\n\n# --- Unit Tests ---\n\n\nclass TestBetSettlementHappyPath:\n    """Tests for successful settlement scenarios."""\n\n    @patch.object(DistributedLock, "acquire", return_value=True)\n    @patch.object(DistributedLock, "release")\n    def test_settle_winning_bet(\n        self, mock_release, mock_acquire, bet_repo, payout_gateway\n    ):\n        service = BetSettlementService(\n            bet_repository=bet_repo,\n            payout_gateway=payout_gateway,\n        )\n\n        result = service.settle_bet("bet-001", outcome="won")\n\n        assert result.settled is True\n        assert result.status == "settled"\n        assert result.payout_amount == Decimal("125.00")\n        assert len(payout_gateway.calls) == 1\n        assert payout_gateway.calls[0]["amount"] == Decimal("125.00")\n        assert bet_repo.get_bet("bet-001").status == "settled"\n\n    @patch.object(DistributedLock, "acquire", return_value=True)\n    @patch.object(DistributedLock, "release")\n    def test_settle_losing_bet(\n        self, mock_release, mock_acquire, bet_repo, payout_gateway\n    ):\n        service = BetSettlementService(\n            bet_repository=bet_repo,\n            payout_gateway=payout_gateway,\n        )\n\n        result = service.settle_bet("bet-001", outcome="lost")\n\n        assert result.settled is True\n        assert result.payout_amount == Decimal("0")\n        assert len(payout_gateway.calls) == 0\n        assert bet_repo.get_bet("bet-001").status == "settled"\n\n\nclass TestIdempotency:\n    """Tests that duplicate settlement is handled gracefully."""\n\n    @patch.object(DistributedLock, "acquire", return_value=True)\n    @patch.object(DistributedLock, "release")\n    def test_already_settled_bet_returns_early(\n        self, mock_release, mock_acquire, bet_repo, payout_gateway\n    ):\n        # Pre-settle the bet\n        bet_repo._bets["bet-001"].status = "settled"\n\n        service = BetSettlementService(\n            bet_repository=bet_repo,\n            payout_gateway=payout_gateway,\n        )\n\n        result = service.settle_bet("bet-001", outcome="won")\n\n        assert result.settled is False\n        assert result.status == "already_settled"\n        assert len(payout_gateway.calls) == 0\n\n\nclass TestConcurrentSettlement:\n    """\n    Regression test for the reported race condition.\n\n    Simulates two settlement events arriving within <500ms for the same bet.\n    Asserts that only ONE payout is processed.\n    """\n\n    def test_concurrent_events_produce_single_payout(self, bet_repo, payout_gateway):\n        """\n        Reproduces the exact scenario from the bug report:\n        - Final whistle event and VAR correction event arrive simultaneously\n        - Both try to settle bet-001\n        - Only one should succeed in processing a payout\n        """\n        service = BetSettlementService(\n            bet_repository=bet_repo,\n            payout_gateway=payout_gateway,\n        )\n\n        results: list[Optional[SettlementResult]] = [None, None]\n        errors: list[Optional[Exception]] = [None, None]\n\n        def settle_worker(index: int) -> None:\n            try:\n                with patch.object(DistributedLock, "acquire", return_value=True):\n                    with patch.object(DistributedLock, "release"):\n                        results[index] = service.settle_bet("bet-001", outcome="won")\n            except Exception as e:\n                errors[index] = e\n\n        t1 = threading.Thread(target=settle_worker, args=(0,))\n        t2 = threading.Thread(target=settle_worker, args=(1,))\n\n        t1.start()\n        t2.start()\n        t1.join()\n        t2.join()\n\n        # Count successful settlements\n        settled_count = sum(\n            1 for r in results if r is not None and r.settled is True\n        )\n        payout_count = len(payout_gateway.calls)\n\n        # CRITICAL ASSERTION: At most one payout\n        assert payout_count <= 1, (\n            f"DUPLICATE PAYOUT DETECTED: {payout_count} payouts processed "\n            f"for the same bet. This is the bug we are fixing."\n        )\n        assert settled_count <= 1\n\n\nclass TestErrorHandling:\n    """Tests for error scenarios."""\n\n    @patch.object(DistributedLock, "acquire", return_value=True)\n    @patch.object(DistributedLock, "release")\n    def test_bet_not_found_raises(self, mock_release, mock_acquire, payout_gateway):\n        empty_repo = FakeBetRepository(bets={})\n        service = BetSettlementService(\n            bet_repository=empty_repo,\n            payout_gateway=payout_gateway,\n        )\n\n        with pytest.raises(BetNotFoundError):\n            service.settle_bet("nonexistent-bet", outcome="won")\n\n    @patch.object(DistributedLock, "acquire", return_value=True)\n    @patch.object(DistributedLock, "release")\n    def test_invalid_state_raises(\n        self, mock_release, mock_acquire, bet_repo, payout_gateway\n    ):\n        bet_repo._bets["bet-001"].status = "voided"\n        service = BetSettlementService(\n            bet_repository=bet_repo,\n            payout_gateway=payout_gateway,\n        )\n\n        with pytest.raises(InvalidBetStateError):\n            service.settle_bet("bet-001", outcome="won")\n\n    @patch.object(DistributedLock, "acquire", return_value=True)\n    @patch.object(DistributedLock, "release")\n    def test_payout_failure_raises(\n        self, mock_release, mock_acquire, bet_repo\n    ):\n        failing_gateway = FakePayoutGateway(should_succeed=False)\n        service = BetSettlementService(\n            bet_repository=bet_repo,\n            payout_gateway=failing_gateway,\n        )\n\n        with pytest.raises(PayoutProcessingError):\n            service.settle_bet("bet-001", outcome="won")\n\n        # Bet status should NOT be updated on payout failure\n        assert bet_repo.get_bet("bet-001").status == "pending"\n\n    def test_lock_acquisition_failure(self, bet_repo, payout_gateway):\n        service = BetSettlementService(\n            bet_repository=bet_repo,\n            payout_gateway=payout_gateway,\n        )\n\n        with patch.object(\n            DistributedLock,\n            "acquire",\n            side_effect=DistributedLockError("bet-settlement:bet-001", 3),\n        ):\n            with pytest.raises(DistributedLockError):\n                service.settle_bet("bet-001", outcome="won")\n\n        # No payout should have been processed\n        assert len(payout_gateway.calls) == 0\n