"""\nModule: settlement_service\nDescription: Bet settlement service with distributed locking to prevent\n    duplicate payouts from concurrent match result events. Implements\n    race-free idempotency using DynamoDB conditional writes.\nAuthor: AI Virtual Engineer\nDate: 2026-08-26\nJira: SCRUM-2\n"""\n\nimport time\nfrom decimal import Decimal\nfrom typing import Optional\n\nimport structlog\n\nfrom config import SettlementConfig\nfrom distributed_lock import DistributedLock\nfrom exceptions import (\n    BetNotFoundError,\n    InvalidBetStateError,\n    PayoutProcessingError,\n)\nfrom models import Bet, BetStatus, PayoutResult\n\nlogger = structlog.get_logger(__name__)\n\n\nclass BetSettlementService:\n    """Service responsible for settling bets and processing payouts.\n\n    Uses a DynamoDB-backed distributed lock to ensure at-most-once\n    payout semantics even when multiple settlement events arrive\n    concurrently for the same bet.\n\n    Attributes:\n        bet_repository: Data access layer for bet records.\n        payout_gateway: Gateway for processing customer payouts.\n        lock: Distributed lock instance for mutual exclusion.\n    """\n\n    def __init__(\n        self,\n        bet_repository: object,\n        payout_gateway: object,\n        lock: Optional[DistributedLock] = None,\n    ) -> None:\n        """Initialize the BetSettlementService.\n\n        Args:\n            bet_repository: Repository for reading/writing bet records.\n            payout_gateway: Gateway for issuing payouts to customers.\n            lock: Optional pre-configured DistributedLock instance.\n                Creates a default instance if not provided.\n        """\n        self.bet_repository = bet_repository\n        self.payout_gateway = payout_gateway\n        self.lock = lock or DistributedLock()\n\n    def settle_bet(\n        self,\n        bet_id: str,\n        match_result: str,\n        event_source: str = \"unknown\",\n    ) -> PayoutResult:\n        """Settle a bet and process payout if the bet is a winner.\n\n        Acquires a distributed lock on the bet before processing to\n        prevent duplicate payouts from concurrent settlement events.\n        The lock is held through the entire read -> decide -> payout ->\n        update sequence.\n\n        Args:\n            bet_id: Unique identifier of the bet to settle.\n            match_result: The final match result used to determine\n                the bet outcome.\n            event_source: Source of the settlement event (e.g.,\n                \"final_whistle\", \"var_correction\").\n\n        Returns:\n            PayoutResult indicating the outcome of the settlement.\n\n        Raises:\n            BetNotFoundError: If the bet does not exist.\n            InvalidBetStateError: If the bet is in an unexpected state.\n            PayoutProcessingError: If the payout fails after settlement.\n            DistributedLockError: If the lock cannot be acquired.\n        """\n        lock_resource = f\"bet_settlement:{bet_id}\"\n        owner_id = None\n\n        logger.info(\n            \"settlement_initiated\",\n            bet_id=bet_id,\n            match_result=match_result,\n            event_source=event_source,\n            extra={\n                \"audit\": True,\n                \"event_type\": \"settlement_initiated\",\n                \"bet_id\": bet_id,\n                \"event_source\": event_source,\n                \"timestamp\": int(time.time()),\n            },\n        )\n\n        try:\n            # Acquire per-bet distributed lock\n            owner_id = self.lock.acquire(resource_id=lock_resource)\n\n            # Re-read bet inside the lock (defence-in-depth)\n            bet = self.bet_repository.get_bet(bet_id)\n            if bet is None:\n                raise BetNotFoundError(\n                    bet_id=bet_id,\n                    message=f\"Bet '{bet_id}' not found\",\n                )\n\n            # Idempotency check inside the lock — race-free\n            if bet.status != BetStatus.PENDING:\n                logger.info(\n                    \"settlement_skipped_already_processed\",\n                    bet_id=bet_id,\n                    current_status=bet.status.value,\n                    event_source=event_source,\n                    extra={\n                        \"audit\": True,\n                        \"event_type\": \"settlement_skipped\",\n                        \"bet_id\": bet_id,\n                        \"reason\": \"already_processed\",\n                        \"current_status\": bet.status.value,\n                        \"timestamp\": int(time.time()),\n                    },\n                )\n                return PayoutResult(\n                    bet_id=bet_id,\n                    status=\"skipped\",\n                    reason=\"already_processed\",\n                    payout_amount=Decimal(\"0\"),\n                )\n\n            # Determine outcome\n            is_winner = self._evaluate_outcome(bet, match_result)\n\n            if is_winner:\n                payout_amount = self._calculate_payout(bet)\n                previous_status = bet.status.value\n\n                # Process payout\n                try:\n                    self.payout_gateway.process_payout(\n                        customer_id=bet.customer_id,\n                        amount=payout_amount,\n                        bet_id=bet_id,\n                    )\n                except Exception as exc:\n                    raise PayoutProcessingError(\n                        bet_id=bet_id,\n                        amount=payout_amount,\n                        message=(\n                            f\"Payout failed for bet '{bet_id}': \"\n                            f\"{str(exc)}\"\n                        ),\n                    ) from exc\n\n                # Update bet status to settled\n                bet.status = BetStatus.SETTLED_WON\n                self.bet_repository.update_bet(bet)\n\n                # UKGC audit log — post-payout\n                logger.info(\n                    \"settlement_completed_payout_issued\",\n                    bet_id=bet_id,\n                    customer_id=bet.customer_id,\n                    match_id=bet.match_id,\n                    payout_amount=str(payout_amount),\n                    previous_status=previous_status,\n                    new_status=bet.status.value,\n                    event_source=event_source,\n                    extra={\n                        \"audit\": True,\n                        \"event_type\": \"payout_issued\",\n                        \"bet_id\": bet_id,\n                        \"customer_id\": bet.customer_id,\n                        \"match_id\": bet.match_id,\n                        \"payout_amount\": str(payout_amount),\n                        \"previous_status\": previous_status,\n                        \"new_status\": bet.status.value,\n                        \"actor\": \"settlement_engine\",\n                        \"timestamp\": int(time.time()),\n                    },\n                )\n\n                return PayoutResult(\n                    bet_id=bet_id,\n                    status=\"settled_won\",\n                    reason=\"payout_issued\",\n                    payout_amount=payout_amount,\n                )\n\n            else:\n                # Losing bet — update status, no payout\n                previous_status = bet.status.value\n                bet.status = BetStatus.SETTLED_LOST\n                self.bet_repository.update_bet(bet)\n\n                logger.info(\n                    \"settlement_completed_no_payout\",\n                    bet_id=bet_id,\n                    customer_id=bet.customer_id,\n                    previous_status=previous_status,\n                    new_status=bet.status.value,\n                    extra={\n                        \"audit\": True,\n                        \"event_type\": \"settlement_no_payout\",\n                        \"bet_id\": bet_id,\n                        \"customer_id\": bet.customer_id,\n                        \"previous_status\": previous_status,\n                        \"new_status\": bet.status.value,\n                        \"actor\": \"settlement_engine\",\n                        \"timestamp\": int(time.time()),\n                    },\n                )\n\n                return PayoutResult(\n                    bet_id=bet_id,\n                    status=\"settled_lost\",\n                    reason=\"bet_lost\",\n                    payout_amount=Decimal(\"0\"),\n                )\n\n        finally:\n            # Always release the lock\n            if owner_id:\n                self.lock.release(\n                    resource_id=lock_resource,\n                    owner_id=owner_id,\n                )\n\n    def _evaluate_outcome(self, bet: Bet, match_result: str) -> bool:\n        """Evaluate whether the bet is a winner based on match result.\n\n        Args:\n            bet: The bet to evaluate.\n            match_result: The final match result string.\n\n        Returns:\n            True if the bet is a winner, False otherwise.\n        """\n        return bet.selection == match_result\n\n    def _calculate_payout(self, bet: Bet) -> Decimal:\n        """Calculate the payout amount for a winning bet.\n\n        Args:\n            bet: The winning bet.\n\n        Returns:\n            The payout amount (stake * odds).\n        """\n        return bet.stake * bet.odds\n